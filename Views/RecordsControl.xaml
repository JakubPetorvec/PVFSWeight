<UserControl x:Class="VUKVWeightApp.Views.RecordsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource NeonPanel}" Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,10" LastChildFill="True">
                <StackPanel Orientation="Horizontal">
                    <Button Content="{Binding RecordingButtonText}"
                            Command="{Binding ToggleRecordingCommand}"
                            Style="{StaticResource NeonButton}"
                            Margin="0,0,10,0"/>
                    <Button Content="Vymazat záznamy"
                            Command="{Binding ClearSamplesCommand}"
                            Style="{StaticResource NeonButton}"
                            Margin="0,0,10,0"/>
                    <Button Content="Exportovat"
                            Command="{Binding ExportSamplesCommand}"
                            Style="{StaticResource NeonButton}" />
                </StackPanel>

                <TextBlock DockPanel.Dock="Right"
                           Text="{Binding TotalWeightText}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Fg}"
                           VerticalAlignment="Center"/>
            </DockPanel>

            <Border Grid.Row="1" CornerRadius="14" Background="Transparent" BorderBrush="{StaticResource StrokeSoft}" BorderThickness="1">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <DataGrid ItemsSource="{Binding Samples}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Style="{StaticResource NeonDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="S1 čas" Binding="{Binding Scale1RxTime}" Width="140"/>
                            <DataGridTextColumn Header="S2 čas" Binding="{Binding Scale2RxTime}" Width="140"/>
                            <DataGridTextColumn Header="Celkem [kg]" Binding="{Binding TotalKg}" Width="110"/>
                            <DataGridTextColumn Header="Váha1 [kg]" Binding="{Binding Scale1Kg}" Width="110"/>
                            <DataGridTextColumn Header="Váha2 [kg]" Binding="{Binding Scale2Kg}" Width="110"/>
                            <DataGridTextColumn Header="G1 [kg]" Binding="{Binding G1Kg}" Width="100"/>
                            <DataGridTextColumn Header="G2 [kg]" Binding="{Binding G2Kg}" Width="100"/>
                            <DataGridTextColumn Header="G3 [kg]" Binding="{Binding G3Kg}" Width="100"/>
                            <DataGridTextColumn Header="G4 [kg]" Binding="{Binding G4Kg}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
