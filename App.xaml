<Application x:Class="VUKVWeightApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">

  <Application.Resources>
    <ResourceDictionary>

      <!-- Colors / Brushes -->
      <SolidColorBrush x:Key="Bg" Color="#0B0F19"/>
      <SolidColorBrush x:Key="Panel" Color="#0F1626"/>
      <SolidColorBrush x:Key="Card" Color="#111827"/>
      <SolidColorBrush x:Key="Card2" Color="#0B1220"/>

      <SolidColorBrush x:Key="Stroke" Color="#2B3A5C"/>
      <SolidColorBrush x:Key="StrokeSoft" Color="#24324F"/>

      <SolidColorBrush x:Key="Text" Color="#F3F6FF"/>
      <SolidColorBrush x:Key="Muted" Color="#B7C2DA"/>
      <SolidColorBrush x:Key="Faint" Color="#7F8AA3"/>

      <!-- Compatibility aliases used by MainWindow.xaml -->
      <SolidColorBrush x:Key="Fg" Color="#F3F6FF"/>
      <SolidColorBrush x:Key="FgMuted" Color="#B7C2DA"/>

      <SolidColorBrush x:Key="Accent" Color="#5B8CFF"/>
      <SolidColorBrush x:Key="Good" Color="#34D399"/>
      <SolidColorBrush x:Key="Warn" Color="#FBBF24"/>
      <SolidColorBrush x:Key="Bad" Color="#FB7185"/>

      <!-- Scrollbar colors -->
      <SolidColorBrush x:Key="ScrollTrack" Color="#0F1626"/>
      <SolidColorBrush x:Key="ScrollThumb" Color="#24324F"/>
      <SolidColorBrush x:Key="ScrollThumbHover" Color="#2E3F63"/>
      <SolidColorBrush x:Key="ScrollThumbPressed" Color="#1E2B46"/>

      <!-- Progress colors -->
      <SolidColorBrush x:Key="ProgTrack" Color="#13203A"/>
      <SolidColorBrush x:Key="ProgFill" Color="#5B8CFF"/>


      <!-- Window defaults -->
      <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Bg}"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
      </Style>

      <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
      </Style>


      <!-- Card style (stronger shadow) -->
      <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Card}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
          <Setter.Value>
            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.28" Color="#000000"/>
          </Setter.Value>
        </Setter>
      </Style>


      <!-- Buttons (base) -->
      <Style TargetType="Button">
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="Margin" Value="6,0"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="Background" Value="#1C2A47"/>
        <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
          <Setter.Value>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.25" Color="#000000"/>
          </Setter.Value>
        </Setter>

        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="b"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="8">
                <ContentPresenter
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
              </Border>

              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="b" Property="Background" Value="#23365C"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="b" Property="Background" Value="#182645"/>
                  <Setter TargetName="b" Property="Effect">
                    <Setter.Value>
                      <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.18" Color="#000000"/>
                    </Setter.Value>
                  </Setter>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="b" Property="Opacity" Value="0.45"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource Accent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
      </Style>

      <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#3A1F2A"/>
        <Setter Property="BorderBrush" Value="#5A2B3B"/>
      </Style>

      <Style x:Key="ToolButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="Margin" Value="6,0"/>
      </Style>


      <!-- TextBox (base) -->
      <Style TargetType="TextBox">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Background" Value="{StaticResource Card2}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Text}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Accent}"/>
      </Style>


      <!-- Thumb template (rounded) -->
      <Style TargetType="Thumb">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Thumb">
              <Border x:Name="thumbBorder"
                      Background="{StaticResource ScrollThumb}"
                      CornerRadius="6"
                      Margin="2"/>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource ScrollThumbHover}"/>
                </Trigger>
                <Trigger Property="IsDragging" Value="True">
                  <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource ScrollThumbPressed}"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>


      <!-- DARK SCROLLBARS -->
      <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource ScrollTrack}"/>
        <Setter Property="Width" Value="12"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ScrollBar">
              <Grid Background="{TemplateBinding Background}">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0"/>
                  </Track.DecreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb/>
                  </Track.Thumb>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0"/>
                  </Track.IncreaseRepeatButton>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>


      <!-- ProgressBar (dark track, no white) -->
      <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource ProgTrack}"/>
        <Setter Property="Foreground" Value="{StaticResource ProgFill}"/>
        <Setter Property="Height" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
      </Style>


      <!-- DataGrid (base) -->
      <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Card}"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="RowBackground" Value="{StaticResource Card}"/>
        <Setter Property="AlternatingRowBackground" Value="#13203A"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeight" Value="30"/>
        <Setter Property="ColumnHeaderHeight" Value="34"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
      </Style>

      <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#1A2A4C"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeSoft}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,0,10,0"/>
      </Style>

      <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0,8,0"/>
      </Style>


      <!-- Style aliases expected by MainWindow.xaml -->
      <Style x:Key="NeonButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"/>
      <Style x:Key="NeonTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"/>
      <Style x:Key="NeonProgress" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}"/>
      <Style x:Key="NeonDataGrid" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}"/>

      <Style x:Key="NeonPanel" TargetType="Border" BasedOn="{StaticResource CardBorder}">
        <Setter Property="Padding" Value="14"/>
      </Style>

    </ResourceDictionary>
  </Application.Resources>
</Application>
